name: desafio
description: A new Flutter project.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  easy_localization: ^3.0.0
  
  flutter:
    sdk: flutter

dev_dependencies:
  pedantic: ^1.11.1

  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true

  assets:
    - assets/images/